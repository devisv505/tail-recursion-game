# Intermediate output from tools/dump_blocks.lua — data/blocks.json is what
# ships, and it is produced by merging this into it.
data/blocks.generated.json

# sitemap.xml is committed, not ignored. It was ignored while the site had no
# domain and the URL had to come from the deployment; now that CNAME pins it to
# tailrecursiongame.com there is nothing to guess, and committing it means the
# file is served whichever way Pages is set to deploy.

# Local preview server config, machine-specific paths.
.claude/launch.json

.DS_Store
Thumbs.db
node_modules/
